RewriteEngine On

# Use environment-aware RewriteBase
# For DigitalOcean (root deployment), use /
# For local XAMPP, use /apartment-rental-system/backend/
RewriteBase /

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]