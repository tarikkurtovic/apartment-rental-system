RewriteEngine On

# RewriteBase is / for production (DigitalOcean)
# For local XAMPP in subdirectory, the base path is handled by Flight framework
RewriteBase /

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]